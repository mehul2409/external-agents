We're building pr-impact for BTW Buildathon Track 3 in this fork: a PR check that reports which other repos a change breaks, for microservice codebases. It's already implemented in ci/pr-impact/ and documented in BUILDATHON.md — read both. The graph emits cross-repo edges as relation_scope: external with full module paths; we resolve those against each repo's exported surface. Verified finding: ParseClaims in entireio/auth-go breaks 5 call sites in entireio/cli. Confirm you understand the architecture, then commit so we get our first checkpoint.